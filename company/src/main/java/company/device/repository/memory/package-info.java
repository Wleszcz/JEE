/**
 * Devices related repositories implementations. In-memory implementations are based on in-memory data storage and
 * do not use any external database.
 */
package company.device.repository.memory;
